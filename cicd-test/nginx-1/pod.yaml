apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginx
spec:
  containers:
  - name: nginx
    image: jejuonul-api-dev.kr.ncr.ntruss.com/nginx
    ports:
    - containerPort: 80
  imagePullSecrets:
  - name: ncr-secret

